// imedate javascript work

// function main(port) {
//     const p = new Promise((res, rej) => {
//         if (port != 5000) {
//             setTimeout(() => res(`server work in port ${port}`), 1000)
//         }else {
//             setTimeout(() => rej(`can't run server in port of ${port}`), 1000)
//         }
//     });

//     return p
// }


// x = main(5000);
// x.then(data => console.log(data))
// .catch(data => console.log(data))

// 'app', { name: 'Mesrop' } in console

// const p = Promise.resolve({ name: 'Mesrop' })
// async function app() {
//     console.log('app');
// }

// app()
// p.then(data => console.log(data))